{
  "name": "bearai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "backend:dev": "npm run start:dev -w backend",
    "backend:build": "npm run build -w backend",
    "backend:start": "npm run start -w backend",
    "miniprogram:dev": "微信开发者工具 --project miniprogram",
    "install:all": "npm install",
    "clean": "rm -rf node_modules && rm -rf backend/node_modules"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
